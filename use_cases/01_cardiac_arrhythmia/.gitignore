__pycache__/
*.pyc
# Raw MIT-BIH downloads (~100 MB if full DB) — reproducible via
# python/download_mitbih.py, never committed.
data/raw/
# Per-record preprocessed CSVs from real PhysioNet records —
# reproducible via python/preprocess.py. The synthetic
# sample_record.csv stays committed and is regenerated by the same
# script with --synthetic.
data/processed/train/
data/processed/test/
