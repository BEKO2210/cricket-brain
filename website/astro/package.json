{
  "name": "cricket-brain-landing",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest",
    "test:ci": "vitest run",
    "ci": "npm run check && npm run test:ci && npm run build"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "astro": "^6.1.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@types/node": "^25.5.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.5"
  },
  "overrides": {
    "yaml": "^2.8.3"
  }
}
