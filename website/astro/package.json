{
  "name": "cricket-brain-landing",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest",
    "test:ci": "vitest run",
    "ci": "npm run check && npm run test:ci && npm run build"
  },
  "dependencies": {
    "astro": "^5.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@types/node": "^25.5.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
