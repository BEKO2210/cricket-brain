[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "cricket-brain"
version = "1.0.0rc1"
description = "Python bindings for cricket-brain"
requires-python = ">=3.9"

[tool.maturin]
module-name = "cricket_brain"
bindings = "pyo3"
